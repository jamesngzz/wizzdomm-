# ============================================================================
# Teacher Assistant v2.0 - Production Requirements
# Image-centric exam grading system with AI vision models
# ============================================================================

# Core Web Framework (keep original tested version)
streamlit==1.29.0

# Streamlit Extensions (keep original tested versions)
streamlit-cropper==0.2.1
streamlit-drawable-canvas==0.9.3

# Database & ORM (keep original tested version)
sqlalchemy==2.0.23

# AI & Machine Learning APIs (keep original tested versions)
openai==1.55.3
google-genai

# Image Processing (keep original tested version)
pillow==10.1.0

# Configuration Management (keep original tested version)
python-dotenv==1.0.0

# Production Database Support (stable version for PostgreSQL)
psycopg2-binary==2.9.9